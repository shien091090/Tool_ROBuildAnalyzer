# M1 匯入工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通「DRO client data.grf → GRF解包 → lub反編譯 → SQLite中間資料庫(data/ro_items.db)」的匯入CLI, 含健康指標report與data.grf指紋。

**Architecture:** 大部分模組自 `D:\Git\SNShienRODataBase\src\rodb\` 原樣移植(含測試), 包名改為 `importer`; 新寫的部分只有 config(json檔化)、db(新schema: items/combos/enchant_tables/import_meta)、fingerprint、pipeline、cli。效果lua保留原文存DB, 不在匯入階段解析。

**Tech Stack:** Python 3.11+(本機用 `py -3.12-64`), pytest, sqlite3(標準庫), 外部工具 luadec.exe / unluac.jar(java)。

**Spec:** `docs/superpowers/specs/2026-09-03-robuildanalyzer-design.md`

## Global Constraints

- 授權GPL-3.0(移植ROItemSearchApp程式碼), repo根目錄須有LICENSE
- 「不默默丟資料」: 任何解析丟棄/異常都要在report留計數
- 中間DB是純衍生物, `data/*.db` 進 .gitignore
- commit message格式: `[{前綴}] [RO配裝分析工具] [{次要功能,非必要}] {內容}`, 一行, 內容用中文敘述不貼程式識別字, 多項目用 `1.XXXX 2.XXXX`
- 所有指令在 `D:\Git\Tool_ROBuildAnalyzer` 下執行, python一律用 `py -3.12-64`
- 測試執行: `py -3.12-64 -m pytest tests/ -v`
- console輸出中文時前綴 `PYTHONIOENCODING=utf-8`

## File Structure

```
Tool_ROBuildAnalyzer/
├─ pyproject.toml
├─ LICENSE                      # GPL-3.0全文
├─ .gitignore
├─ config.example.json
├─ importer/                    # Python package
│   ├─ __init__.py
│   ├─ config.py                # 新寫: 讀config.json
│   ├─ grf.py                   # 移植: GRF解包
│   ├─ lua_scan.py              # 移植: 括號/end配對掃描
│   ├─ decompile.py             # 移植: luadec/unluac wrapper
│   ├─ fingerprint.py           # 新寫: data.grf指紋
│   ├─ db.py                    # 新寫: schema DDL + insert helpers
│   ├─ pipeline.py              # 新寫: 解包→反編譯→解析→寫DB→report
│   ├─ cli.py                   # 新寫: python -m importer.cli
│   └─ parsers/
│       ├─ __init__.py
│       ├─ iteminfo.py          # 移植
│       ├─ itemdbname.py        # 移植
│       ├─ equipment_properties.py  # 移植(含parse_combiitem)
│       └─ enchant.py           # 移植
├─ data/
│   └─ tools/                   # luadec.exe, unluac.jar(自ROItemSearchApp APP/複製)
└─ tests/
    ├─ __init__.py
    ├─ test_grf.py              # 移植
    ├─ test_lua_scan.py         # 移植
    ├─ test_decompile.py        # 移植
    ├─ test_iteminfo.py         # 移植
    ├─ test_itemdbname.py       # 移植
    ├─ test_equipment_properties.py  # 移植
    ├─ test_enchant.py          # 移植
    ├─ test_config.py           # 新寫
    ├─ test_fingerprint.py      # 新寫
    ├─ test_db.py               # 新寫
    └─ test_pipeline.py         # 新寫
```

移植來源對照(改import路徑 `rodb` → `importer`, 其餘不動邏輯):

| 目標 | 來源 |
|---|---|
| importer/grf.py | D:\Git\SNShienRODataBase\src\rodb\grf.py |
| importer/lua_scan.py | src\rodb\lua_scan.py |
| importer/decompile.py | src\rodb\decompile.py |
| importer/parsers/iteminfo.py | src\rodb\parsers\iteminfo.py |
| importer/parsers/itemdbname.py | src\rodb\parsers\itemdbname.py |
| importer/parsers/equipment_properties.py | src\rodb\parsers\equipment_properties.py |
| importer/parsers/enchant.py | src\rodb\parsers\enchant.py |
| tests/test_*.py(上述各對應) | SNShienRODataBase\tests\ 同名檔 |

---

### Task 1: repo scaffold與外部工具

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `LICENSE`, `config.example.json`, `importer/__init__.py`, `importer/parsers/__init__.py`, `tests/__init__.py`, `data/tools/luadec.exe`, `data/tools/unluac.jar`

**Interfaces:**
- Produces: 可跑 `py -3.12-64 -m pytest tests/` 的空專案骨架; `data/tools/` 兩支反編譯工具

- [ ] **Step 1: 建立pyproject.toml**

```toml
[project]
name = "robuildanalyzer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
```

- [ ] **Step 2: 建立.gitignore**

```
__pycache__/
*.pyc
.venv/
data/*.db
data/lub/
data/lua/
config.json
```

- [ ] **Step 3: 放LICENSE(GPL-3.0全文)**

從 `C:\Users\lithoshu\Desktop\ROItemSearchApp-0.7.17-260816\LICENSE` 複製全文(即GPL-3.0標準文本)。

- [ ] **Step 4: 建立config.example.json**

```json
{
  "dro_path": "D:\\倉庫\\其他\\DRO",
  "java_exe": "java",
  "db_path": "data/ro_items.db"
}
```

- [ ] **Step 5: 複製外部工具**

```bash
mkdir -p data/tools
cp "/c/Users/lithoshu/Desktop/ROItemSearchApp-0.7.17-260816/APP/luadec.exe" data/tools/
cp "/c/Users/lithoshu/Desktop/ROItemSearchApp-0.7.17-260816/APP/unluac.jar" data/tools/
```

- [ ] **Step 6: 建立空的`importer/__init__.py`、`importer/parsers/__init__.py`、`tests/__init__.py`, 跑pytest確認收集0個測試不報錯**

Run: `py -3.12-64 -m pytest tests/ -v`
Expected: `no tests ran`(exit code 5是pytest無測試的正常代碼, 不是錯)

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml .gitignore LICENSE config.example.json importer tests data/tools
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 1.建立專案骨架與GPL-3.0授權 2.導入反編譯外部工具"
```
(外部工具導入屬plug性質, 但與骨架同commit以feat為主前綴即可)

---

### Task 2: 移植底層模組(grf / lua_scan / decompile)

**Files:**
- Create: `importer/grf.py`, `importer/lua_scan.py`, `importer/decompile.py`
- Test: `tests/test_grf.py`, `tests/test_lua_scan.py`, `tests/test_decompile.py`

**Interfaces:**
- Produces:
  - `grf.build_index(grf_path: str) -> dict[str, GrfEntry]`(key為小寫反斜線路徑, cp949解碼)
  - `grf.extract(grf_path: str, entry: GrfEntry) -> bytes`
  - `lua_scan.find_matching_brace(text: str, open_idx: int) -> int`
  - `lua_scan.find_matching_end(text: str, search_from: int) -> int`
  - `decompile.run_luadec(luadec_exe, lub_path, out_path) -> None`
  - `decompile.run_unluac(java_exe, unluac_jar, lub_path, out_path) -> None`

- [ ] **Step 1: 複製三支模組與對應測試**

從移植來源對照表複製6個檔案, 把測試與模組內所有 `from rodb.` / `import rodb` 改為 `from importer.` / `import importer`。邏輯一行都不改。

- [ ] **Step 2: 跑測試**

Run: `py -3.12-64 -m pytest tests/test_grf.py tests/test_lua_scan.py tests/test_decompile.py -v`
Expected: 全PASS(來源repo這三份測試本來就全過; 若有測試依賴真實DRO路徑而環境缺檔, 該測試在來源repo的寫法就是skip, 保持原樣)

- [ ] **Step 3: Commit**

```bash
git add importer/grf.py importer/lua_scan.py importer/decompile.py tests/test_grf.py tests/test_lua_scan.py tests/test_decompile.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 移植GRF解包、lua掃描與反編譯模組"
```

---

### Task 3: 移植parsers(iteminfo / itemdbname / equipment_properties / enchant)

**Files:**
- Create: `importer/parsers/iteminfo.py`, `importer/parsers/itemdbname.py`, `importer/parsers/equipment_properties.py`, `importer/parsers/enchant.py`
- Test: `tests/test_iteminfo.py`, `tests/test_itemdbname.py`, `tests/test_equipment_properties.py`, `tests/test_enchant.py`

**Interfaces:**
- Produces:
  - `iteminfo.parse(text) -> list[dict]` — keys: item_id, display_name, description_lines(list[str]), slot_count, class_num, effect_id
  - `itemdbname.parse(text) -> dict[str, int]` — internal_name → item_id
  - `equipment_properties.parse(text) -> list[dict]` — keys: item_id, equip_type, stat_vector, onstart_equip_src, combi_item_ids
  - `equipment_properties.parse_combiitem(text) -> dict[int, dict]` — combo_id → {member_item_ids, onstart_src}
  - `enchant.parse(text) -> list[dict]` — keys: table_index, target_internal_names(list), slot_index, require_cost(str), success_rate(int), option_internal_name, option_weight

- [ ] **Step 1: 複製四支parser與對應測試, import路徑 `rodb` → `importer`**

- [ ] **Step 2: 跑測試**

Run: `py -3.12-64 -m pytest tests/test_iteminfo.py tests/test_itemdbname.py tests/test_equipment_properties.py tests/test_enchant.py -v`
Expected: 全PASS

- [ ] **Step 3: Commit**

```bash
git add importer/parsers tests/test_iteminfo.py tests/test_itemdbname.py tests/test_equipment_properties.py tests/test_enchant.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 移植道具資訊、內部名稱對照、裝備效果與附魔表解析器"
```

---

### Task 4: config讀取(config.json)

**Files:**
- Create: `importer/config.py`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces:
  - `config.load(path: str = "config.json") -> Config`
  - `Config` dataclass: 欄位 `dro_path: str`, `java_exe: str`, `db_path: str`; property `data_grf_path`(= dro_path/data.grf), `iteminfo_lub_path`(= dro_path/System/iteminfo_new.lub); 常數 `LUADEC_EXE = "data/tools/luadec.exe"`, `UNLUAC_JAR = "data/tools/unluac.jar"`(repo內固定路徑, 不進config)
  - 檔案不存在時 raise `ConfigNotFoundError`(自訂例外), 訊息含「請複製 config.example.json 為 config.json」
- Consumes: 無

- [ ] **Step 1: 寫failing test**

```python
# tests/test_config.py
import json
import pytest
from importer import config


def test_load_reads_fields(tmp_path):
    p = tmp_path / "config.json"
    p.write_text(json.dumps({
        "dro_path": "D:\\DRO", "java_exe": "java", "db_path": "data/x.db"
    }), encoding="utf-8")
    cfg = config.load(str(p))
    assert cfg.dro_path == "D:\\DRO"
    assert cfg.java_exe == "java"
    assert cfg.db_path == "data/x.db"
    assert cfg.data_grf_path.endswith("data.grf")
    assert cfg.iteminfo_lub_path.endswith("iteminfo_new.lub")


def test_load_missing_file_raises_with_hint(tmp_path):
    with pytest.raises(config.ConfigNotFoundError) as e:
        config.load(str(tmp_path / "nope.json"))
    assert "config.example.json" in str(e.value)
```

- [ ] **Step 2: 跑測試確認fail**

Run: `py -3.12-64 -m pytest tests/test_config.py -v`
Expected: FAIL(module has no attribute / import error)

- [ ] **Step 3: 實作**

```python
# importer/config.py
import json
import os
from dataclasses import dataclass

LUADEC_EXE = "data/tools/luadec.exe"
UNLUAC_JAR = "data/tools/unluac.jar"


class ConfigNotFoundError(Exception):
    pass


@dataclass
class Config:
    dro_path: str
    java_exe: str
    db_path: str

    @property
    def data_grf_path(self) -> str:
        return os.path.join(self.dro_path, "data.grf")

    @property
    def iteminfo_lub_path(self) -> str:
        return os.path.join(self.dro_path, "System", "iteminfo_new.lub")


def load(path: str = "config.json") -> Config:
    if not os.path.exists(path):
        raise ConfigNotFoundError(
            f"找不到設定檔 {path}, 請複製 config.example.json 為 config.json 並填入DRO路徑"
        )
    with open(path, encoding="utf-8") as f:
        raw = json.load(f)
    return Config(
        dro_path=raw["dro_path"],
        java_exe=raw.get("java_exe", "java"),
        db_path=raw.get("db_path", "data/ro_items.db"),
    )
```

- [ ] **Step 4: 跑測試確認pass**

Run: `py -3.12-64 -m pytest tests/test_config.py -v`
Expected: 2 PASS

- [ ] **Step 5: Commit**

```bash
git add importer/config.py tests/test_config.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 新增設定檔讀取, 缺檔時提示複製範本"
```

---

### Task 5: data.grf指紋

**Files:**
- Create: `importer/fingerprint.py`
- Test: `tests/test_fingerprint.py`

**Interfaces:**
- Produces: `fingerprint.of_file(path: str) -> str` — 格式 `"{size}:{mtime_ns}"`; 檔案不存在raise `FileNotFoundError`
- Consumes: 無(App端M5比對時用同一函式)

- [ ] **Step 1: 寫failing test**

```python
# tests/test_fingerprint.py
import os
import pytest
from importer import fingerprint


def test_fingerprint_is_size_and_mtime(tmp_path):
    p = tmp_path / "a.grf"
    p.write_bytes(b"hello")
    fp = fingerprint.of_file(str(p))
    size, mtime_ns = fp.split(":")
    assert int(size) == 5
    assert int(mtime_ns) == os.stat(p).st_mtime_ns


def test_fingerprint_changes_when_content_grows(tmp_path):
    p = tmp_path / "a.grf"
    p.write_bytes(b"hello")
    fp1 = fingerprint.of_file(str(p))
    p.write_bytes(b"hello world")
    assert fingerprint.of_file(str(p)) != fp1


def test_missing_file_raises(tmp_path):
    with pytest.raises(FileNotFoundError):
        fingerprint.of_file(str(tmp_path / "nope.grf"))
```

- [ ] **Step 2: 跑測試確認fail**

Run: `py -3.12-64 -m pytest tests/test_fingerprint.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# importer/fingerprint.py
import os


def of_file(path: str) -> str:
    st = os.stat(path)
    return f"{st.st_size}:{st.st_mtime_ns}"
```

- [ ] **Step 4: 跑測試確認pass**

Run: `py -3.12-64 -m pytest tests/test_fingerprint.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add importer/fingerprint.py tests/test_fingerprint.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 新增遊戲資料檔指紋計算, 供更新偵測比對"
```

---

### Task 6: DB schema與寫入

**Files:**
- Create: `importer/db.py`
- Test: `tests/test_db.py`

**Interfaces:**
- Produces:
  - `db.create(conn: sqlite3.Connection) -> None` — 建4張表(已存在則先DROP重建, 匯入一律全量重寫)
  - `db.insert_items(conn, rows: list[dict]) -> None` — row keys: item_id, internal_name, display_name, description(str), slot_count, class_num, equip_type, stat_vector(list|None), onstart_equip_src(str|None), combi_ids(list|None); list欄位以`json.dumps`存TEXT
  - `db.insert_combos(conn, combos: dict[int, dict]) -> None` — value keys: member_item_ids(list), onstart_src(str|None)
  - `db.insert_enchants(conn, rows: list[dict]) -> None` — row keys同 enchant.parse 輸出, target_internal_names以json存
  - `db.set_meta(conn, key: str, value: str) -> None` / `db.get_meta(conn, key: str) -> str | None`
- Consumes: Task 3 各parser的輸出dict形狀

- [ ] **Step 1: 寫failing test**

```python
# tests/test_db.py
import json
import sqlite3
from importer import db


def _conn():
    c = sqlite3.connect(":memory:")
    db.create(c)
    return c


def test_create_builds_four_tables():
    c = _conn()
    names = {r[0] for r in c.execute(
        "SELECT name FROM sqlite_master WHERE type='table'")}
    assert {"items", "combos", "enchant_tables", "import_meta"} <= names


def test_insert_items_roundtrip_json_fields():
    c = _conn()
    db.insert_items(c, [{
        "item_id": 450263, "internal_name": "Lunar_E_Armor_LT",
        "display_name": "月全蝕魔力外袍-LT", "description": "系列 : 鎧甲",
        "slot_count": 1, "class_num": 2, "equip_type": "armor",
        "stat_vector": [10, 0, 2], "onstart_equip_src": "function() end",
        "combi_ids": [2000002553],
    }])
    row = c.execute(
        "SELECT display_name, stat_vector, combi_ids FROM items WHERE item_id=450263"
    ).fetchone()
    assert row[0] == "月全蝕魔力外袍-LT"
    assert json.loads(row[1]) == [10, 0, 2]
    assert json.loads(row[2]) == [2000002553]


def test_insert_items_none_fields_stay_null():
    c = _conn()
    db.insert_items(c, [{
        "item_id": 1, "internal_name": None, "display_name": None,
        "description": "", "slot_count": None, "class_num": None,
        "equip_type": None, "stat_vector": None,
        "onstart_equip_src": None, "combi_ids": None,
    }])
    row = c.execute("SELECT stat_vector, combi_ids FROM items WHERE item_id=1").fetchone()
    assert row == (None, None)


def test_insert_combos():
    c = _conn()
    db.insert_combos(c, {2000000007: {
        "member_item_ids": [4244, 4299], "onstart_src": "function() end"}})
    row = c.execute(
        "SELECT member_item_ids, onstart_src FROM combos WHERE combo_id=2000000007"
    ).fetchone()
    assert json.loads(row[0]) == [4244, 4299]
    assert row[1] == "function() end"


def test_insert_enchants():
    c = _conn()
    db.insert_enchants(c, [{
        "table_index": 10004, "target_internal_names": ["Lunar_E_Armor_LT"],
        "slot_index": 1, "require_cost": '0, {"Pow_Meteorite_Fragment", 5}',
        "success_rate": 100000, "option_internal_name": "Star_Cluster_Of_Pow1",
        "option_weight": 13600,
    }])
    row = c.execute(
        "SELECT target_internal_names, option_weight FROM enchant_tables"
    ).fetchone()
    assert json.loads(row[0]) == ["Lunar_E_Armor_LT"]
    assert row[1] == 13600


def test_meta_roundtrip():
    c = _conn()
    db.set_meta(c, "grf_fingerprint", "123:456")
    assert db.get_meta(c, "grf_fingerprint") == "123:456"
    assert db.get_meta(c, "nope") is None


def test_create_twice_resets():
    c = _conn()
    db.insert_items(c, [{
        "item_id": 1, "internal_name": None, "display_name": None,
        "description": "", "slot_count": None, "class_num": None,
        "equip_type": None, "stat_vector": None,
        "onstart_equip_src": None, "combi_ids": None,
    }])
    db.create(c)
    assert c.execute("SELECT COUNT(*) FROM items").fetchone()[0] == 0
```

- [ ] **Step 2: 跑測試確認fail**

Run: `py -3.12-64 -m pytest tests/test_db.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# importer/db.py
import json
import sqlite3

_DDL = """
DROP TABLE IF EXISTS items;
DROP TABLE IF EXISTS combos;
DROP TABLE IF EXISTS enchant_tables;
DROP TABLE IF EXISTS import_meta;
CREATE TABLE items(
  item_id INTEGER PRIMARY KEY,
  internal_name TEXT,
  display_name TEXT,
  description TEXT,
  slot_count INTEGER,
  class_num INTEGER,
  equip_type TEXT,
  stat_vector TEXT,
  onstart_equip_src TEXT,
  combi_ids TEXT
);
CREATE TABLE combos(
  combo_id INTEGER PRIMARY KEY,
  member_item_ids TEXT NOT NULL,
  onstart_src TEXT
);
CREATE TABLE enchant_tables(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  table_index INTEGER NOT NULL,
  target_internal_names TEXT NOT NULL,
  slot_index INTEGER NOT NULL,
  require_cost TEXT,
  success_rate INTEGER,
  option_internal_name TEXT NOT NULL,
  option_weight INTEGER NOT NULL
);
CREATE TABLE import_meta(
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
"""


def _j(v):
    return json.dumps(v, ensure_ascii=False) if v is not None else None


def create(conn: sqlite3.Connection) -> None:
    conn.executescript(_DDL)


def insert_items(conn: sqlite3.Connection, rows: list[dict]) -> None:
    conn.executemany(
        "INSERT INTO items(item_id, internal_name, display_name, description,"
        " slot_count, class_num, equip_type, stat_vector, onstart_equip_src, combi_ids)"
        " VALUES(:item_id, :internal_name, :display_name, :description,"
        " :slot_count, :class_num, :equip_type, :stat_vector, :onstart_equip_src, :combi_ids)",
        [{**r, "stat_vector": _j(r["stat_vector"]), "combi_ids": _j(r["combi_ids"])}
         for r in rows],
    )


def insert_combos(conn: sqlite3.Connection, combos: dict) -> None:
    conn.executemany(
        "INSERT INTO combos(combo_id, member_item_ids, onstart_src) VALUES(?,?,?)",
        [(cid, _j(v["member_item_ids"]), v["onstart_src"]) for cid, v in combos.items()],
    )


def insert_enchants(conn: sqlite3.Connection, rows: list[dict]) -> None:
    conn.executemany(
        "INSERT INTO enchant_tables(table_index, target_internal_names, slot_index,"
        " require_cost, success_rate, option_internal_name, option_weight)"
        " VALUES(:table_index, :target_internal_names, :slot_index,"
        " :require_cost, :success_rate, :option_internal_name, :option_weight)",
        [{**r, "target_internal_names": _j(r["target_internal_names"])} for r in rows],
    )


def set_meta(conn: sqlite3.Connection, key: str, value: str) -> None:
    conn.execute(
        "INSERT INTO import_meta(key, value) VALUES(?,?)"
        " ON CONFLICT(key) DO UPDATE SET value=excluded.value",
        (key, value),
    )


def get_meta(conn: sqlite3.Connection, key: str):
    row = conn.execute("SELECT value FROM import_meta WHERE key=?", (key,)).fetchone()
    return row[0] if row else None
```

- [ ] **Step 4: 跑測試確認pass**

Run: `py -3.12-64 -m pytest tests/test_db.py -v`
Expected: 7 PASS

- [ ] **Step 5: Commit**

```bash
git add importer/db.py tests/test_db.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 新增中間資料庫schema與寫入層"
```

---

### Task 7: pipeline(合併items + report健康指標)

**Files:**
- Create: `importer/pipeline.py`
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: Task 3 parsers、Task 6 db
- Produces: `pipeline.run(lua_texts: dict[str, str], db_path: str, grf_fingerprint: str) -> dict`
  - `lua_texts` keys: `"iteminfo"`, `"itemdbname"`, `"equipment_properties"`, `"enchant"`(皆為反編譯後的lua文字; 檔案IO與反編譯在cli層做, pipeline只吃文字, 方便測試)
  - 回傳report dict, keys至少含: `items_count`, `items_with_effect_count`, `items_missing_display_name_count`, `combos_count`, `enchant_rules_count`, `enchant_tables_count`, `enchant_weight_anomaly_count`
  - items合併規則: 以iteminfo的item_id全集為基底, itemdbname反轉(id→internal_name)補internal_name, equipment_properties補equip_type/stat_vector/onstart_equip_src/combi_ids; description = description_lines用`"\n"`join
  - `enchant_weight_anomaly_count` = 權重總和≠500000的(table_index, slot_index)組數(依盤點已知有100000/600000/514000等變體, 屬預期資料型態, 只記錄不擋匯入; 引擎端一律用實際總和當分母)

- [ ] **Step 1: 寫failing test**

```python
# tests/test_pipeline.py
import json
import sqlite3
from importer import pipeline

_ITEMINFO = """
tbl = {
  [450263] = {
    identifiedDisplayName = "月全蝕魔力外袍-LT",
    identifiedDescriptionName = { "第一行", "第二行" },
    slotCount = 1,
    ClassNum = 2,
  },
  [999999] = {
    slotCount = 0,
    ClassNum = 0,
  },
}
"""

_ITEMDBNAME = """
Lunar_E_Armor_LT = 450263
"""

_EQUIP_PROPS = """
Item = {
  [450263] = {
    Type = "armor",
    Stat = { 10, 0, 2 },
    OnStartEquip = function()
      AddDamage_CRI(1, 7)
    end,
    Combiitem = { 2000000007 }
  }
}
Combiitem = {
  [2000000007] = {
    Item = {450263, 4299},
    OnStartEquip = function()
      AddExtParam(0, 242, 2)
    end
  }
}
"""

_ENCHANT = """
Table[10004] = CreateEnchantInfo()
Table[10004]:AddTargetItem("Lunar_E_Armor_LT")
Table[10004].Slot[1]:SetRequire(0, {"Pow_Meteorite_Fragment", 5})
Table[10004].Slot[1]:SetSuccessRate(100000)
Table[10004].Slot[1]:SetEnchant(1, "Star_Cluster_Of_Pow1", 300000)
Table[10004].Slot[1]:SetEnchant(1, "Star_Cluster_Of_Crt1", 200000)
Table[10005] = CreateEnchantInfo()
Table[10005]:AddTargetItem("Other_Item")
Table[10005].Slot[2]:SetRequire(0, {"X", 1})
Table[10005].Slot[2]:SetSuccessRate(100000)
Table[10005].Slot[2]:SetEnchant(1, "A_1", 60000)
Table[10005].Slot[2]:SetEnchant(1, "B_1", 40000)
"""


def _run(tmp_path):
    db_path = str(tmp_path / "t.db")
    report = pipeline.run(
        {
            "iteminfo": _ITEMINFO,
            "itemdbname": _ITEMDBNAME,
            "equipment_properties": _EQUIP_PROPS,
            "enchant": _ENCHANT,
        },
        db_path,
        "123:456",
    )
    return report, sqlite3.connect(db_path)


def test_items_merged_across_sources(tmp_path):
    report, conn = _run(tmp_path)
    row = conn.execute(
        "SELECT internal_name, display_name, description, slot_count,"
        " equip_type, stat_vector, onstart_equip_src, combi_ids"
        " FROM items WHERE item_id=450263").fetchone()
    assert row[0] == "Lunar_E_Armor_LT"
    assert row[1] == "月全蝕魔力外袍-LT"
    assert row[2] == "第一行\n第二行"
    assert row[3] == 1
    assert row[4] == "armor"
    assert json.loads(row[5]) == [10, 0, 2]
    assert "AddDamage_CRI" in row[6]
    assert json.loads(row[7]) == [2000000007]


def test_item_without_equipment_entry_kept(tmp_path):
    report, conn = _run(tmp_path)
    row = conn.execute(
        "SELECT equip_type, onstart_equip_src FROM items WHERE item_id=999999").fetchone()
    assert row == (None, None)


def test_combos_stored(tmp_path):
    report, conn = _run(tmp_path)
    row = conn.execute(
        "SELECT member_item_ids, onstart_src FROM combos WHERE combo_id=2000000007").fetchone()
    assert json.loads(row[0]) == [450263, 4299]
    assert "AddExtParam" in row[1]


def test_report_counts(tmp_path):
    report, conn = _run(tmp_path)
    assert report["items_count"] == 2
    assert report["items_with_effect_count"] == 1
    assert report["items_missing_display_name_count"] == 1
    assert report["combos_count"] == 1
    assert report["enchant_rules_count"] == 3
    assert report["enchant_tables_count"] == 2
    # table10004 slot1總和=500000正常, table10005 slot2總和=100000 → 1組異常
    assert report["enchant_weight_anomaly_count"] == 1


def test_fingerprint_and_date_written(tmp_path):
    report, conn = _run(tmp_path)
    fp = conn.execute(
        "SELECT value FROM import_meta WHERE key='grf_fingerprint'").fetchone()
    assert fp[0] == "123:456"
    assert conn.execute(
        "SELECT value FROM import_meta WHERE key='import_date'").fetchone() is not None
```

- [ ] **Step 2: 跑測試確認fail**

Run: `py -3.12-64 -m pytest tests/test_pipeline.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# importer/pipeline.py
import datetime
import sqlite3
from collections import defaultdict

from importer import db
from importer.parsers import enchant, equipment_properties, iteminfo, itemdbname


def run(lua_texts: dict[str, str], db_path: str, grf_fingerprint: str) -> dict:
    info_rows = iteminfo.parse(lua_texts["iteminfo"])
    name_to_id = itemdbname.parse(lua_texts["itemdbname"])
    id_to_name = {v: k for k, v in name_to_id.items()}
    equip_rows = {r["item_id"]: r for r in
                  equipment_properties.parse(lua_texts["equipment_properties"])}
    combos = equipment_properties.parse_combiitem(lua_texts["equipment_properties"])
    enchant_rows = enchant.parse(lua_texts["enchant"])

    items = []
    for info in info_rows:
        iid = info["item_id"]
        eq = equip_rows.get(iid, {})
        items.append({
            "item_id": iid,
            "internal_name": id_to_name.get(iid),
            "display_name": info["display_name"],
            "description": "\n".join(info["description_lines"]),
            "slot_count": info["slot_count"],
            "class_num": info["class_num"],
            "equip_type": eq.get("equip_type"),
            "stat_vector": eq.get("stat_vector"),
            "onstart_equip_src": eq.get("onstart_equip_src"),
            "combi_ids": eq.get("combi_item_ids"),
        })

    weight_sums = defaultdict(int)
    for r in enchant_rows:
        weight_sums[(r["table_index"], r["slot_index"])] += r["option_weight"]
    anomaly_count = sum(1 for total in weight_sums.values() if total != 500000)

    conn = sqlite3.connect(db_path)
    try:
        db.create(conn)
        db.insert_items(conn, items)
        db.insert_combos(conn, combos)
        db.insert_enchants(conn, enchant_rows)
        db.set_meta(conn, "grf_fingerprint", grf_fingerprint)
        db.set_meta(conn, "import_date", datetime.date.today().isoformat())
        conn.commit()
    finally:
        conn.close()

    return {
        "items_count": len(items),
        "items_with_effect_count": sum(
            1 for i in items if i["onstart_equip_src"]),
        "items_missing_display_name_count": sum(
            1 for i in items if not i["display_name"]),
        "combos_count": len(combos),
        "enchant_rules_count": len(enchant_rows),
        "enchant_tables_count": len({r["table_index"] for r in enchant_rows}),
        "enchant_weight_anomaly_count": anomaly_count,
    }
```

- [ ] **Step 4: 跑測試確認pass**

Run: `py -3.12-64 -m pytest tests/test_pipeline.py -v`
Expected: 5 PASS

- [ ] **Step 5: Commit**

```bash
git add importer/pipeline.py tests/test_pipeline.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 新增匯入管線, 合併三來源道具資料並輸出健康指標"
```

---

### Task 8: CLI(解包→反編譯→pipeline)

**Files:**
- Create: `importer/cli.py`

**Interfaces:**
- Consumes: Task 2 grf/decompile、Task 4 config、Task 5 fingerprint、Task 7 pipeline
- Produces: `py -3.12-64 -m importer.cli` 端到端執行; 逐行印report
- 此task無單元測試(純IO編排, 各組件已各自有測試), 以Task 9真實資料端到端驗證

- [ ] **Step 1: 實作**

```python
# importer/cli.py
import os

from importer import config, decompile, fingerprint, grf, pipeline

_GRF_LUB_TARGETS = {
    "equipment_properties":
        "data\\luafiles514\\lua files\\equipmentproperties\\equipmentproperties.lub",
    "enchant": "data\\luafiles514\\lua files\\enchant\\enchantlist.lub",
    "itemdbname": "data\\luafiles514\\lua files\\itemdbnametbl.lub",
}
# 反編譯器選用比照SNShienRODataBase驗證過的組合:
# equipment_properties用unluac(luadec對這支會產出壞結構), 其餘用luadec
_UNLUAC_KEYS = {"equipment_properties"}


def main() -> None:
    cfg = config.load()
    os.makedirs("data/lub", exist_ok=True)
    os.makedirs("data/lua", exist_ok=True)
    os.makedirs(os.path.dirname(cfg.db_path) or ".", exist_ok=True)

    print("讀取GRF索引…")
    index = grf.build_index(cfg.data_grf_path)

    lub_paths = {}
    for key, rel in _GRF_LUB_TARGETS.items():
        entry = index[rel]
        lub_path = f"data/lub/{key}.lub"
        with open(lub_path, "wb") as f:
            f.write(grf.extract(cfg.data_grf_path, entry))
        lub_paths[key] = lub_path

    # iteminfo不在GRF裡, 直接拿System資料夾的檔案
    lub_paths["iteminfo"] = cfg.iteminfo_lub_path

    lua_texts = {}
    for key, lub_path in lub_paths.items():
        out_path = f"data/lua/{key}.lua"
        print(f"反編譯 {key}…")
        if key in _UNLUAC_KEYS:
            decompile.run_unluac(cfg.java_exe, config.UNLUAC_JAR, lub_path, out_path)
        else:
            decompile.run_luadec(config.LUADEC_EXE, lub_path, out_path)
        with open(out_path, encoding="utf-8", errors="replace") as f:
            lua_texts[key] = f.read()

    print("解析並寫入資料庫…")
    report = pipeline.run(
        lua_texts, cfg.db_path, fingerprint.of_file(cfg.data_grf_path))
    for k, v in report.items():
        print(f"{k}={v}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 全部測試跑一輪確認沒破**

Run: `py -3.12-64 -m pytest tests/ -v`
Expected: 全PASS

- [ ] **Step 3: Commit**

```bash
git add importer/cli.py
git commit -m "[feat] [RO配裝分析工具] [匯入工具] 新增匯入CLI, 串接解包、反編譯與管線"
```

---

### Task 9: 真實資料端到端驗證

**Files:**
- Create: `config.json`(本機, gitignore不進版控)

**Interfaces:**
- Consumes: 全部前置task

- [ ] **Step 1: 建config.json**

複製config.example.json為config.json。java_exe若環境PATH沒有java, 填 `C:\\Program Files\\Eclipse Adoptium\\jre-21.0.12.101-hotspot\\bin\\java.exe`。

- [ ] **Step 2: 跑匯入**

Run: `PYTHONIOENCODING=utf-8 py -3.12-64 -m importer.cli`
Expected: 正常結束並印出report。

- [ ] **Step 3: 對照歷史基準檢查report數字**

與SNShienRODataBase已知數字同量級即合格(資料版本不同容忍小差):
- `items_count` ≈ 20799
- `items_with_effect_count` ≈ 8842
- `enchant_rules_count` ≈ 18083
- `enchant_tables_count` ≈ 81
- `enchant_weight_anomaly_count` > 0(已知存在100000/600000/514000權重總和的表)
- `combos_count` > 10000(item_sets歷史值11800同量級)

抽查DB內容:

```bash
PYTHONIOENCODING=utf-8 py -3.12-64 -c "
import sqlite3, json
c = sqlite3.connect('data/ro_items.db')
row = c.execute(\"SELECT display_name, slot_count, onstart_equip_src FROM items WHERE item_id=450263\").fetchone()
print(row[0], row[1]); print(row[2][:200])
print(c.execute(\"SELECT COUNT(*) FROM enchant_tables WHERE table_index=10004\").fetchone())
print(c.execute(\"SELECT value FROM import_meta WHERE key='grf_fingerprint'\").fetchone())
"
```

Expected: 月全蝕魔力外袍-LT / slot數 / OnStartEquip原文開頭; table 10004有515筆(90+275+150); 指紋非空。

- [ ] **Step 4: 異常數字時停下回報**

任何指標偏離量級(例如items_with_effect_count掉到千位), 不要硬修, 停下來與使用者對齊再繼續。

- [ ] **Step 5: Commit(若前面步驟有為驗證調整程式碼才需要; 純驗證無變更則跳過)**

---

## Self-Review紀錄

- Spec覆蓋(M1範圍): §2工具鏈移植(Task 2/3)、§4.1四張表(Task 6, combos表為spec本次補充)、§4.2 config.json與dro_path(Task 4; GUI對話框屬M4)、指紋(Task 5)、§4.3健康指標(Task 7)、§12-4別名對照層: **不在M1做** — 官網名稱比對是附魔手動建檔輔助, 屬M3附魔資料範疇, 屆時再議
- 型別一致性: parsers輸出dict keys與pipeline消費、db insert參數已逐一對過
- 無placeholder
